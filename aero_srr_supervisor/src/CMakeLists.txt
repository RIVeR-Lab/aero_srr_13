#Set all parameters here
#Parameters should have 3 set()'s: set(X_ARG arg_literal), set(X_CDEF DEF), set(X_VAL, value)

set(auto_gen_warn "AUTOMATICALLY GENERATED BY CMAKE. DO NOT EDIT BY HAND")

#Namespace params
set(R_NS aero)
set(SP_NS supervisor)


#Paramter defining the AeroState topic
set(STATE_TOPIC_ARG ${R_NS}/${SP_NS}/state)
set(STATE_TOPIC_CDF STATE_TOPIC)
set(STATE_TOPIC_VAL "\"${R_NS}/${SP_NS}/state\"")

#Parameter defining the ControlMode topic
set(CTRLMD_TOPIC_ARG ${R_NS}/${SP_NS}/control_mode)
set(CTRLMD_TOPIC_CDF CTRLMD_TOPIC)
set(CTRLMD_TOPIC_VAL "\"${R_NS}/${SP_NS}/control_mode\"")

#Build the parameter header
configure_file (
  "AeroSupervisorParameters.h.in"
  "${PROJECT_BINARY_DIR}/AeroSupervisorParameters.h"
)
	

add_subdirectory(helpers)
add_subdirectory(nodes)
add_subdirectory(launch)