configure_file (
  "vision_pipeline.launch.in"
  "${PROJECT_SOURCE_DIR}/launch/vision_pipeline.launch"
  )
  
  configure_file (
  "vision_pipeline_boom.launch.in"
  "${PROJECT_SOURCE_DIR}/launch/vision_pipeline_boom.launch"
  )
    configure_file (
  "vision_pipeline_bottom.launch.in"
  "${PROJECT_SOURCE_DIR}/launch/vision_pipeline_bottom.launch"
  )