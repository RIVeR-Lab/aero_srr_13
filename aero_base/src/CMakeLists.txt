set(AUTO_GENERATION_WARNING "AUTOMATICALLY GENERATED BY CMAKE. DO NOT EDIT BY HAND!")
#Set all parameters here
#Parameters should have 3 set()'s: set(X_ARG arg_literal), set(X_CDEF DEF), set(X_VAL, value)

#Namespace params
set(R_NS aero)
set(T_NS transform)
set(T_IMU imu)
set(T_ARM arm_base)
set(T_LFC lower_stereo/left_camera_frame)
set(T_RTC lower_stereo/right_camera_frame)
set(T_BOM boom)
set(T_LDR laser)


#Parameter for the topic for exchanging OccupancyGrids between the global and local planner
set(TF_BASE_NAME_ARG ${R_NS}/${T_NS}/base_frame)
set(TF_BASE_NAME_CDF TF_BASE_NAME)
set(TF_BASE_NAME_VAL "\"robot\"")

set(TF_IMU_NAME_ARG ${R_NS}/${T_NS}/${T_IMU}/frame)
set(TF_IMU_NAME_CDF TF_IMU_NAME)
set(TF_IMU_NAME_VAL "\"${T_IMU}\"")
set(TF_IMU_NAME_ARG ${R_NS}/${T_NS}/${T_IMU}/x)
set(TF_IMU_NAME_VAL "\"${0}\"")
set(TF_IMU_NAME_ARG ${R_NS}/${T_NS}/${T_IMU}/y)
set(TF_IMU_NAME_VAL "\"${0}\"")
set(TF_IMU_NAME_ARG ${R_NS}/${T_NS}/${T_IMU}/z)
set(TF_IMU_NAME_VAL "\"${0}\"")
set(TF_IMU_NAME_ARG ${R_NS}/${T_NS}/${T_IMU}/i)
set(TF_IMU_NAME_VAL "\"${0}\"")
set(TF_IMU_NAME_ARG ${R_NS}/${T_NS}/${T_IMU}/j)
set(TF_IMU_NAME_VAL "\"${0}\"")
set(TF_IMU_NAME_ARG ${R_NS}/${T_NS}/${T_IMU}/k)
set(TF_IMU_NAME_VAL "\"${0}\"")
set(TF_IMU_NAME_ARG ${R_NS}/${T_NS}/${T_IMU}/w)
set(TF_IMU_NAME_VAL "\"${0}\"")

set(TF_ARM_NAME_ARG ${R_NS}/${T_NS}/${T_ARM}/frame)
set(TF_ARM_NAME_CDF TF_ARM_NAME)
set(TF_ARM_NAME_VAL "\"${T_ARM}\"")

set(TF_LCAM_NAME_ARG ${R_NS}/${T_NS}/${T_LFC}/frame)
set(TF_LCAM_NAME_CDF TF_LCAM_NAME)
set(TF_LCAM_NAME_VAL "\"${T_LFC}\"")

set(TF_RCAM_NAME_ARG ${R_NS}/${T_NS}/${T_RTC}/frame)
set(TF_RCAM_NAME_CDF TF_RCAM_NAME)
set(TF_RCAM_NAME_VAL "\"${T_RTC}\"")

set(TF_BOOM_NAME_ARG ${R_NS}/${T_NS}/${T_BOM}/frame)
set(TF_BOOM_NAME_CDF TF_BOOM_NAME)
set(TF_BOOM_NAME_VAL "\"${T_BOM}\"")

set(TF_LIDAR_NAME_ARG ${R_NS}/${T_NS}/${T_LDR}/frame)
set(TF_LIDAR_NAME_CDF TF_LIDAR_NAME)
set(TF_LIDAR_NAME_VAL "\"${T_LDR}\"")


#Build the parameter header
configure_file (
  "AeroBaseParams.h.in"
  "${PROJECT_BINARY_DIR}/AeroBaseParams.h"
)


add_subdirectory(nodes)
add_subdirectory(launch)
add_subdirectory(dox)