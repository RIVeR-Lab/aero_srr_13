set(AUTO_GENERATION_WARNING "AUTOMATICALLY GENERATED BY CMAKE. DO NOT EDIT BY HAND")

#Set all parameters here
#Parameters should have 3 set()'s: set(X_ARG arg_literal), set(X_CDEF DEF), set(X_VAL, value)

#Namespace params
set(R_NS aero)
set(P_NS laser_filter_pipeline)
set(C_NS crop)
set(BL_NS bottom_left)
set(RT_NS top_right)

#Parameter The node manager to use
set(NODE_MANAGER_ARG node_manager)
set(NODE_MANAGER_CDF NODE_MANAGER)
set(NODE_MANAGER_VAL "\"${R_NS}_laser_filter_pipeline\"")

#Parameter for input topic for the front end of the pipeline
set(INPUT_TOPIC_ARG "input_topic")
set(INPUT_TOPIC_CDF INPUT_TOPIC)
set(INPUT_TOPIC_VAL "\"/scan\"")

#Parameter for the raw point cloud topic consumed by the stages
set(POINTCLOUD_ARG "point_cloud_raw")
set(POINTCLOUD_CDF POINTCLOUD)
set(POINTCLOUD_VAL "\"/${R_NS}/${P_NS}/point_cloud_raw\"")

#Parameter for the locally scoped point cloud topic
set(LOCAL_TOPIC_ARG "local_point_cloud")
set(LOCAL_TOPIC_CDF LOCAL_TOPIC)
set(LOCAL_TOPIC_VAL "\"/${R_NS}/local_point_cloud\"")

#Parameter for the globally scoped point cloud topic
set(GLOBAL_TOPIC_ARG "global_point_cloud")
set(GLOBAL_TOPIC_CDF GLOBAL_TOPIC)
set(GLOBAL_TOPIC_VAL "\"/${R_NS}/global_point_cloud\"")


#Parameter for the output frame for the local filter
set(LOCAL_FRAME_ARG "local_point_frame")
set(LOCAL_FRAME_CDF LOCAL_FRAME)
set(LOCAL_FRAME_VAL "\"/base_footprint\"")

#Parameter for the output frame for the global filter
set(GLOBAL_FRAME_ARG "global_point_frame")
set(GLOBAL_FRAME_CDF GLOBAL_FRAME)
set(GLOBAL_FRAME_VAL "\"/world\"")

configure_file (
  "AeroLaserFilterParams.h.in"
  "${PROJECT_BINARY_DIR}/AeroLaserFilterParams.h"
)

add_subdirectory(nodelets)
add_subdirectory(launch)
add_subdirectory(helpers)
